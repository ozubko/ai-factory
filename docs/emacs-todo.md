# Emacs cockpit follow-ups

Possible next improvements after the first cockpit PR:

- add ERT tests for metadata parsing and command construction;
- add a transient menu if the project later accepts a soft dependency or optional integration;
- add log browser helpers for `logs/` and `verify/`;
- add a command to copy `pr-body.md` to the kill ring;
- add a mode-line indicator for the current run's outcome;
- package the file for MELPA-style installation if the frontend becomes stable.
