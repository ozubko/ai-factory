# ai-factory.el

A thin Emacs cockpit for the `ai-factory` CLI.

See [`../docs/emacs.md`](../docs/emacs.md) for setup and usage.

Minimal setup:

```elisp
(add-to-list 'load-path "/path/to/ai-factory/emacs")
(require 'ai-factory)
```

Start with:

```text
M-x ai-factory-list
```
